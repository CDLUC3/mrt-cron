## Dependencies
- Clone the git repo on a host that is authorized to call `aws lambda invoke`
- Ruby
## Install

```
cd consistency-driver
bundle install
```

## Execution
```
cd consistency-driver
ruby driver.rb [env]
```
